framework:
    validation:
        # Active la validation par attributs PHP (#[Assert\...])
        email_validation_mode: html5

when@test:
    framework:
        validation:
            not_compromised_password: false
